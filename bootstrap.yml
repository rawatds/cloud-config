eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost

microservice:
  paymentservice:
    endpoints:
      endpoint:
        uri: http://PAYMENT-SERVICE/payments
        
